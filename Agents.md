# 仓库介绍
**dsh plugin 多项目仓库,仓库间彼此独立**
- vision-bridge 视觉桥接(为无视觉Agent设计)
- dsh-session-ui-enhance dsh 会话优化plugin(导轨/排版/代码块卡片;自 v1.2.0 起融合 mermaid 图表渲染、v1.3.0 起改为浏览器本地 mermaid.js 渲染——会话内 ```mermaid 代码块 fence 闭合即原位替换为可交互 SVG 卡片,零网络依赖;原 dsh-mermaid-renderer 独立插件已废弃移除;v1.4.0 起展开的高思考块底部提供「收起」小字按钮 + zcode 式轮次过程折叠——定稿轮次的思考/工具过程收成一行「过程细节」,只展示最终响应)
- dsh-fs-observation-persistence fs 观测状态持久化plugin(host-only;把每会话的 fs/observed 记录落盘,恢复会话后文件未变即可直接 write/edit 免重读,变过仍被版本 CAS 以 FS_STALE_VERSION 强制重读;intent 决策器 prepend 注册、仅在持久化命中时决策,其余 next() 委托官方 fs-observation-policy,卸载即无缝回到官方行为;v0.2.0 起 fork 会话无自己记录时一跳继承父会话记录,v0.2.1 修正 subagent 判据——凭 origin==='subagent' 或 delegationDepth>0,GUI fork 持久化的 delegationDepth:0 不再被误判;fork 观测到缺失时同步清父记录条目,避免继承死版本导致 write 死循环;可用 inheritForkObservations:false 关闭)
- dsh-tool-bootstrap 工具目录锚定plugin(host-only;挂 system-prompt/assemble 瀑布把 bootstrap 请求的工具目录收窄为 [bash, read](Windows [pwsh, read]),maxTokens 压到 1024,并剥离 skill-catalog/agent-instructions 注入;晋升信号读持久 session events——promoteOn 可配 either/tool-call/assistant-message/first-turn-complete;随附 preset 与上游一致不预发消息(prewarm 可选):用户第一条消息就是 request #1,promoteOn=either——首个持久 tool/call 或 assistant/message 落盘后同一回合的下一步即开放全量工具 + AGENTS.md + skill catalog;全程 system prompt 压成单独的 Minimal 一句话(其余 section 全剥离),原 system prompt 以 persona context 消息在晋升后第一个 pre-step 注入;prewarm 投递经 ctx.setTimeout 延后到 session.append 栈外(inject:[timer]),prewarmed 会话去重,subagent/fork/非空会话跳过,投递前再核对会话仍组成于本 preset;随附 tool-bootstrap-standard(原 Standard persona,system 全程锚定句,原 Standard system prompt 晋升后以 persona context 注入)、tool-bootstrap-cordis(创造 persona/skills,无 cordis_* 工具——tool-cordis 的 Host Cordis Inspect 提供器是进程单例,与官方创造模式同进程冲突)与 tool-bootstrap-zero-standard(标准模式 zero-tool 锚定:第一条真实消息前插固定锚定消息,首个模型请求 0 工具,首个 assistant/message 后开放完整目录)三个用户 preset;独立 id 可与上游 anchored-standard/zero-anchored-standard 同机共存,sync:preset 默认同步 standard/cordis 两个(zero 需 SYNC_ZERO_PRESET=1),SYNC_RELEASE=1(sync:preset:release)时 preset 引用 profile 里已安装的 dsh-tool-bootstrap 发布包(当前 tag dsh-tool-bootstrap-v0.4.0);附 headless e2e profile(需 node-linker=hoisted 平铺避免 dsh-scope 双副本)与参数化驱动器(E2E_PREWARM/E2E_PRESET_ID/E2E_BOOTSTRAP/E2E_FULL_INCLUDES/E2E_SELECT_PATH/E2E_PRE_SELECT/E2E_TURN1_SYSTEM/E2E_TURN1_SYSTEM_CONTAINS/E2E_TURN2_SYSTEM_CONTAINS/E2E_PERSONA_CONTEXT_CONTAINS);rc.6 中 loader 并发激活 preset 行、行序不保证 pre-step listener 注册序,本插件以 prepend 注册 agent/pre-step 监听恒为瀑布最外层做最终过滤;随附 preset 不挂 agent-instructions 行,晋升后由插件自行注入全局+项目 AGENTS.md)
